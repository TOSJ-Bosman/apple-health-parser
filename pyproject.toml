[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "apple-health-parser"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "gspread>=6.0",
    "google-auth>=2.0",
]
