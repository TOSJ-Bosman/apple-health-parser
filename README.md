# apple-health-parser
Python package that parses the export.xml created by the "Export all health data" function in Apple Health. Includes functionalities to export the parsed data to Google Sheets.

## Storing credentials
The credentials to log in to the google sheets should be stored in the `.credentials/` folder.